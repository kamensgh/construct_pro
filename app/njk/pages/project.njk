<!-- website.nunjucks -->
{% set title='Welcome to ConstructPro' %}
{% set topNav = 'about' %}
{% extends "site_base2.njk" %}


{% block stylesheet %}


{% endblock %}




{% block content %}

<div class="mini-banner about">
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-xl-9">
        <h2 class="breadcrumbs-custom-title text-center" data-aos="fade-up" data-aos-once="true"
          data-aos-duration="1000">About</h2>
      </div>
    </div>
  </div>
</div>
<div class="about-container projects">
  <div class="container">
    <div class="row ">
      <div class="col-md-6 col-lg-5 col-xl-4 offset-0 offset-md-1 aos-init aos-animate" data-aos="fade-right"
        data-aos-once="true" data-aos-duration="1000">
        <!-- <span class=" mini-title">About Company</span> -->

        <h3>Mission and Vission</h3>


      </div>
      <div class="col-md-4 col-lg-5 col-xl-6">
        <p class="mt-lg-0">We aim to be a world class Construction Company providing efficient affordable, sustainable,
          cost effective services of highest level of quality. We believe in maintaining the highest standard of
          professionalism, integrity, creativity, positive attitude, delivery that meets an surpasses expectations while
          offering prompts and lasting solutions that stand the test of time.</p>


        <p class="text-primary" style="font-size: 22px;">We envision to be regional market leader in provision of civil engineering and
          construction services.</p>
      </div>
    </div>
  </div>

</div>



<div class="team">

  <h3 class="text-center mb-5">Executive Team</h3>


  <div class="container">

    <div class="row ">
      <div class="col-12 col-md-6">
        <div class="team-inner">
          <img src="../app/dist/images/21.png" class="" alt="...">
          <div class="content">
            <h3>Prof. Felix Hammond</h3>
            <p>Board Chairman</p>
          </div>
        </div>
        <div class="team-inner">
          <img src="../app/dist/images/21.png" class="" alt="...">
          <div class="content inverse">
            <h3>Ernest Amissah</h3>
            <p>Chief Executive Officer</p>
          </div>
        </div>
        <div class="team-inner">
          <img src="../app/dist/images/21.png" class="" alt="...">
          <div class="content">
            <h3>Mathew Amissah JNR</h3>
            <p>Chief Technology Officer</p>
          </div>
        </div>


      </div>
      <div class="col-12 col-md-6">
        <div class="team-inner">
          <img src="../app/dist/images/21.png" class="" alt="...">
          <div class="content inverse">
            <h3>Dr. Ernest Asamoah</h3>
            <p>Board Member</p>
          </div>
        </div>
        <div class="team-inner">
          <img src="../app/dist/images/21.png" class="" alt="...">
          <div class="content">
            <h3>Emmanuel Arthur</h3>
            <p>Project Manager</p>
          </div>
        </div>

      </div>

    </div>

  </div>

</div>





{% endblock %}



{% block scripts %}

<script type="text/javascript">
  var modalId = $('#image-gallery');

  $(document).ready(function () {
    loadGallery(true, 'a.thumbnail');

    //This function disables buttons when needed
    function disableButtons(counter_max, counter_current) {
      $('#show-previous-image, #show-next-image').show();
      if (counter_max === counter_current) {
        $('#show-next-image').hide();
      } else if (counter_current === 1) {
        $('#show-previous-image').hide();
      }
    }

    /**
     *
     * @param setIDs        Sets IDs when DOM is loaded. If using a PHP counter, set to false.
     * @param setClickAttr  Sets the attribute for the click handler.
     */

    function loadGallery(setIDs, setClickAttr) {
      var current_image,
        selector,
        counter = 0;

      $('#show-next-image, #show-previous-image').click(function () {
        if ($(this).attr('id') === 'show-previous-image') {
          current_image--;
        } else {
          current_image++;
        }

        selector = $('[data-image-id="' + current_image + '"]');
        updateGallery(selector);
      });

      function updateGallery(selector) {
        var $sel = selector;
        current_image = $sel.data('image-id');
        $('#image-gallery-title').text($sel.data('title'));
        $('#image-gallery-image').attr('src', $sel.data('image'));
        disableButtons(counter, $sel.data('image-id'));
      }

      if (setIDs == true) {
        $('[data-image-id]').each(function () {
          counter++;
          $(this).attr('data-image-id', counter);
        });
      }
      $(setClickAttr).on('click', function () {
        updateGallery($(this));
      });
    }
  });

  // build key actions
  $(document).keydown(function (e) {
    switch (e.which) {
      case 37: // left
        if (
          (modalId.data('bs.modal') || {})._isShown &&
          $('#show-previous-image').is(':visible')
        ) {
          $('#show-previous-image').click();
        }
        break;

      case 39: // right
        if (
          (modalId.data('bs.modal') || {})._isShown &&
          $('#show-next-image').is(':visible')
        ) {
          $('#show-next-image').click();
        }
        break;

      default:
        return; // exit this handler for other keys
    }
    e.preventDefault(); // prevent the default action (scroll / move caret)
  });
</script>



{% endblock %}